# Redacted migration fixture

This directory contains only synthetic, credential-free input used by the
portable migration tests. Tests create the SQLite database at runtime so no
platform-specific native database artifact is checked in.

